export interface Todo {
  id?: string;
  content: string;
  priority?:string;
  datemodified?: Date;
  isDone?: boolean;


  // content: string;
  // id?: string;
  // datemodified?: Date;
  // priority?:string;
  // isDone?: boolean;
}
